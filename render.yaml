services:
  - type: web
    name: studiodigimix
    runtime: static
    buildCommand: pnpm install && pnpm run build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html